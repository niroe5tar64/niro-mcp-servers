---
description: インタラクティブまたは自動モードのクイック仕様生成
allowed-tools: Read, SlashCommand, TodoWrite, Bash, Write, Glob
argument-hint: <project-description> [--auto]
---

# クイック仕様生成

<background_information>
- **使命**: 1 コマンドで全フェーズ（init → requirements → design → tasks）を実行する
- **成功基準**:
  - インタラクティブモード: 各フェーズでユーザーが進行を承認できる
  - 自動モード: `--auto` 指定時はすべてのフェーズを中断なく実行
  - 生成品質は手動ワークフローと同等
</background_information>

<instructions>
## ⚠️ 重要: 自動モード実行ルール

**`$ARGUMENTS` に `--auto` が含まれる場合、自動モード。**

自動モードでは:
- 4 フェーズすべてを連続実行
- TodoWrite で進捗を追跡（4 タスク: init, requirements, design, tasks）
- 各フェーズ完了で TodoWrite を更新し直ちに次へ進む
- フェーズ 2-4 の「次のステップ」メッセージは**無視**する（単独利用向けのため）
- フェーズ 4 完了またはエラー時のみ停止

**TodoWrite による進捗管理**:
- フェーズ 1 完了 = 1/4 → フェーズ 2 続行
- フェーズ 2 完了 = 2/4 → フェーズ 3 続行
- フェーズ 3 完了 = 3/4 → フェーズ 4 続行
- フェーズ 4 完了 = 4/4 → サマリーを出力して終了

---

## コアタスク
4 フェーズを順番に実行する。自動モードでは停止せずに全フェーズを実行する。インタラクティブモードではフェーズ間で承認を求める。

## 実行手順

### Step 1: 引数解析と初期化

`$ARGUMENTS` を解析:
- `--auto` が含まれる → **自動モード**（4 フェーズを連続実行）
- それ以外 → **インタラクティブモード**（各フェーズで確認）
- 説明文を抽出（`--auto` を除去）

例:
```
"User profile with avatar upload --auto" → mode=automatic, description="User profile with avatar upload"
"User profile feature" → mode=interactive, description="User profile feature"
```

**TodoWrite のタスクリストを作成**:
```json
[
  {"content": "Initialize spec", "activeForm": "Initializing spec", "status": "pending"},
  {"content": "Generate requirements", "activeForm": "Generating requirements", "status": "pending"},
  {"content": "Generate design", "activeForm": "Generating design", "status": "pending"},
  {"content": "Generate tasks", "activeForm": "Generating tasks", "status": "pending"}
]
```

モードバナーを表示し、Step 2 へ進む。

### Step 2: フェーズループを実行

以下の 4 フェーズを順に実行:

---

#### フェーズ 1: 仕様初期化（直接実装）

**TodoWrite 更新**: タスク 1 を `in_progress` に。

**コアロジック**:

1. **機能名の生成**:
   - 説明文を kebab-case に変換
   - 例: "User profile with avatar upload" → "user-profile-avatar-upload"
   - 簡潔に（理想は 2-4 語）

2. **一意性の確認**:
   - Glob で `.kiro/specs/*/` を確認
   - 既存なら `-2`, `-3` を付与

3. **ディレクトリ作成**:
   - Bash: `mkdir -p .kiro/specs/{feature-name}`

4. **テンプレートからファイル初期化**:

   a. テンプレートを読む:
   ```
   - .kiro/settings/templates/specs/init.json
   - .kiro/settings/templates/specs/requirements-init.md
   ```

   b. プレースホルダ置換:
   ```
   {{FEATURE_NAME}} → feature-name
   {{TIMESTAMP}} → 現在の ISO 8601 タイムスタンプ（`date -u +"%Y-%m-%dT%H:%M:%SZ"`）
   {{PROJECT_DESCRIPTION}} → description
   ```

   c. Write ツールで書き込み:
   ```
   - .kiro/specs/{feature-name}/spec.json
   - .kiro/specs/{feature-name}/requirements.md
   ```

5. **TodoWrite 更新**: タスク 1 を `completed`、タスク 2 を `in_progress` に。

6. **進捗出力**:
   ```
   ✅ Spec initialized at .kiro/specs/{feature-name}/
   ```

**自動モード**: 直ちにフェーズ 2 へ。

**インタラクティブモード**: "要件生成へ進みますか? (yes/no)" を表示
- "no": 現在の状態を表示して停止
- "yes": フェーズ 2 へ

---

#### フェーズ 2: 要件生成

**タスク 2 はフェーズ 1 で `in_progress` になっている。**

**SlashCommand 実行**:
```
/kiro:spec-requirements {feature-name}
```

完了まで待機。Subagent は「次のステップ」メッセージを返す。

**重要**: 自動モードでは「次のステップ」メッセージを**無視**する。

**TodoWrite 更新**: タスク 2 を `completed`、タスク 3 を `in_progress` に。

**進捗出力**:
```
✅ Requirements generated → Continuing to design...
```

**自動モード**: タスク 2/4 完了。直ちにフェーズ 3 へ。

**インタラクティブモード**: "設計生成へ進みますか? (yes/no)" を表示
- "no": 現在の状態を表示して停止
- "yes": フェーズ 3 へ

---

#### フェーズ 3: 設計生成

**タスク 3 はフェーズ 2 で `in_progress` になっている。**

**SlashCommand 実行**:
```
/kiro:spec-design {feature-name} -y
```

注意: `-y` で要件を自動承認する。

完了まで待機。Subagent は「次のステップ」メッセージを返す。

**重要**: 自動モードでは「次のステップ」メッセージを**無視**する。

**TodoWrite 更新**: タスク 3 を `completed`、タスク 4 を `in_progress` に。

**進捗出力**:
```
✅ Design generated → Continuing to tasks...
```

**自動モード**: タスク 3/4 完了。直ちにフェーズ 4 へ。

**インタラクティブモード**: "タスク生成へ進みますか? (yes/no)" を表示
- "no": 現在の状態を表示して停止
- "yes": フェーズ 4 へ

---

#### フェーズ 4: タスク生成

**タスク 4 はフェーズ 3 で `in_progress` になっている。**

**SlashCommand 実行**:
```
/kiro:spec-tasks {feature-name} -y
```

注意: `-y` で設計を自動承認する。

完了まで待機。

**TodoWrite 更新**: タスク 4 を `completed` に。

**4/4 完了。ループ終了。**

最終サマリー（出力説明参照）を表示して終了。

---

## 重要な制約

### フェーズ 1 実装メモ
- 機能名生成は決定的で読みやすいものにする
- ディレクトリ作成前に競合を必ず確認
- テンプレートの存在を検証してから読み込む
- タイムスタンプは ISO 8601 形式: `YYYY-MM-DDTHH:MM:SSZ`

### 自動モードの挙動
- フェーズ間で停止しない
- ユーザー入力を待たない
- フェーズ 2-4 の「次のステップ」メッセージに影響されない
- フェーズごとに TodoWrite を更新し進捗を可視化
- 4 フェーズ完了まで継続

### インタラクティブモードの挙動
- 各フェーズ後にユーザー確認
- "yes/y" または "no/n" を待つ
- "no": 現在の状態を表示して停止
- "yes": 次フェーズへ

### エラーハンドリング
- どのフェーズでも失敗したらワークフローを停止
- エラーと現在状態を表示
- 手動復旧コマンドを提案

</instructions>

## ツールガイダンス

### フェーズ 1 のツール
- **Glob**: `.kiro/specs/*/` で既存の機能名を確認
- **Bash**: `mkdir -p` でディレクトリ作成、`date -u` でタイムスタンプ生成
- **Read**: `.kiro/settings/templates/specs/` からテンプレート取得
- **Write**: spec.json と requirements.md を書き込み

### フェーズ 2-4 のツール
- **SlashCommand**: `/kiro:spec-requirements`, `/kiro:spec-design`, `/kiro:spec-tasks` を実行

### TodoWrite の使い方
- 4 つの pending タスクで初期化
- 各フェーズ後に更新: 現在タスクを `completed`、次タスクを `in_progress`
- UI 上での進捗可視化を提供

## 出力説明

### モードバナー

**インタラクティブモード**:
```
🚀 Quick Spec Generation (Interactive Mode)

You will be prompted at each phase.
⚠️ Skips gap analysis and design validation.
```

**自動モード**:
```
🚀 Quick Spec Generation (Automatic Mode)

All phases execute automatically without prompts.
⚠️ Skips all validations and reviews.
```

### 中間出力

各フェーズ後に簡潔な進捗を表示:
```
✅ Spec initialized at .kiro/specs/{feature}/
✅ Requirements generated → Continuing to design...
✅ Design generated → Continuing to tasks...
```

### 最終完了サマリー

spec.json で指定された言語で出力:

```
✅ Quick Spec Generation Complete!

## Generated Files:
- .kiro/specs/{feature}/spec.json
- .kiro/specs/{feature}/requirements.md ({X} requirements)
- .kiro/specs/{feature}/design.md ({Y} components, {Z} endpoints)
- .kiro/specs/{feature}/tasks.md ({N} tasks)

⚠️ Quick generation skipped:
- `/kiro:validate-gap` - Gap analysis (integration check)
- `/kiro:validate-design` - Design review (architecture validation)

## Next Steps:
1. Review generated specs (especially design.md)
2. Optional validation:
   - `/kiro:validate-gap {feature}` - Check integration with existing codebase
   - `/kiro:validate-design {feature}` - Verify architecture quality
3. Start implementation: `/kiro:spec-impl {feature}`

## Note:
For complex features (integrations, security, APIs), use standard workflow:
/kiro:spec-init → /kiro:spec-requirements → /kiro:validate-gap
→ /kiro:spec-design → /kiro:validate-design → /kiro:spec-tasks
```

## セーフティ & フォールバック

### 引数解析
- `$1`, `$2` ではなく `$ARGUMENTS` を使う
- 説明文にスペースが含まれても正しく扱う
- 例: "Multi word description --auto" → 両方を正しく抽出

### 機能名生成
- 小文字の kebab-case へ変換
- 特殊文字は削除
- 曖昧な場合は短さより説明的を優先
- 競合時は `-2`, `-3` を付与

### エラーシナリオ

**テンプレート欠如**:
- `.kiro/settings/templates/specs/` の存在を確認
- 欠落ファイルを明示
- エラーで終了

**ディレクトリ作成失敗**:
- 権限を確認
- エラーパスを表示
- 終了

**フェーズ実行失敗**（フェーズ 2-4）:
- ワークフロー停止
- 現在状態と完了済みフェーズを表示
- 提案: "Continue manually from `/kiro:spec-{next-phase} {feature}`"

**ユーザー中断**（インタラクティブモード）:
- 丁寧に停止
- 完了済みフェーズを表示
- 手動継続を提案

### 使用ガイダンス

**自動モード**（`--auto`）が適する場合:
- 単純機能（CRUD、基本 UI）
- プロトタイピング/PoC
- 既知の機能パターン

**インタラクティブモード**（デフォルト）が適する場合:
- 初めて spec-quick を使う
- 各フェーズをレビューしたい
- 中程度の複雑な機能

**標準ワークフロー**を使うべき場合（spec-quick は使わない）:
- 既存システムとの複雑な統合
- セキュリティ重要機能
- 本番品質が必要
- ギャップ分析や設計検証が必要
