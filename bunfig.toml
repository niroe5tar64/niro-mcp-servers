# Bun configuration for monorepo

[install]
# Enable workspace support
peer = true
# Hoist dependencies to root node_modules
auto = "auto"
# Use binary lockfile format
lockfile = "bun.lockb"

[test]
# Test configuration
coverage = true
