{
  "mcpServers": {
    "confluence-md": {
      "command": "docker",
      "cwd": "${workspaceFolder}",
      "args": [
        "compose",
        "-f",
        "docker-compose.yml",
        "run",
        "--rm",
        "confluence-md"
      ]
    }
  }
}
