{
  "name": "@niro-mcp/confluence-md",
  "version": "0.1.0",
  "private": true,
  "description": "Confluence to Markdown MCP Server",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@niro-mcp/confluence-cleaner": "workspace:*",
    "@niro-mcp/mcp-server-core": "workspace:*",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
